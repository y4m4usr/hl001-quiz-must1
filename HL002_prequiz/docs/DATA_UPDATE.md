# データ更新手順（作成予定）

1. `questions/questions.json` を編集し、画像URL・選択肢を更新します。
2. 画像は `assets/` もしくは GitHub Raw で管理します。
3. Apps Script 側に変更がある場合は `gas/` フォルダを編集し `clasp push` を実行します。
4. 更新後はテストチェックリストに沿って動作確認を行ってください。

