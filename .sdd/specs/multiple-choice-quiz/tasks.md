# 実装タスクリスト

## 1. マークアップ整理
- [ ] `src/index.html` を HL002 仕様に合わせて再構成する（home/quiz/result の 3 セクションを単一ドキュメントで定義し、重複している旧プレースホルダー HTML を削除する）。
- [ ] セクション内のテキストとボタンラベルを最新ガイドライン（日本語表記・10問固定）に統一し、アクセシビリティ属性（`aria-live` 等）を適切に配置する。

## 2. スタイル調整
- [ ] `src/styles/main.css` を見直し、レイアウト/ボタン/レスポンシブ動作を HL002 用に簡素化（ホーム・クイズ・結果の共通レイアウト、ボタンサイズ、余白、モバイル幅対応）する。
- [ ] 画像エリアの縦横比維持とフォールバック表示を CSS で保証し、欠損時にも崩れないスタイルを設定する。

## 3. クイズロジック整備
- [ ] `src/scripts/main.js` の初期化処理・状態管理を整理し、`startGame`→`renderQuestion`→`showResult` のフローとリセット処理を明瞭にする。
- [ ] `questions/questions.json` からのデータ取得時にシャッフルと 10 件抽出を実装し、例外発生時のユーザ通知（ロード失敗/質問不足）を見直す。
- [ ] 選択肢クリック後の正誤表示・次問遷移・最終結果計算を確認し、ボタン状態リセットとクラス付けを最新仕様に合わせて調整する。

## 4. データ・アセット整備
- [ ] `questions/questions.json` を HL002 研修用の最新フォーマットで整備し、最低 10 問分の `imageUrl` / `choices` / `answer` をダミーデータ含めて用意する。
- [x] 必要なロゴやプレースホルダー画像を `assets/` または `image/` に整理し、HTML/CSS/JS からの参照パスを更新する。

## 5. ドキュメント更新
- [ ] `docs/README.md`・`docs/TEST_CHECKLIST.md`・`docs/DATA_UPDATE.md` などの運用ドキュメントを HL002 仕様に合わせて更新し、手順やチェック項目を反映する。
- [ ] `AGENTS.md` との整合性を確認し、必要に応じて追加の注意事項（JSON 検証コマンド等）を追記する。

## 6. 動作確認
- [ ] `python -m http.server 4173 --directory src` などでローカルプレビューを起動し、PC Chrome とモバイル（エミュレータ可）で 10 問通しの動作と画像フォールバックを検証する。
- [ ] `docs/TEST_CHECKLIST.md` の各項目を実施し、確認結果を PR もしくは記録メモに残す。


